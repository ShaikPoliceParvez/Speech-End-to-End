# Core dependencies
faster-whisper
langfuse==4.14.2
num2words
numpy
ollama
opencv-python
pydantic
pydantic-settings
pillow
piper-tts
psutil
python-dotenv
sounddevice
soundfile
supertonic

# Optional: IndicConformer for higher-accuracy Hindi / Telugu / Malayalam STT
# Requires STT_INDIC_ASR_ENABLED = True in config/stt_config.py (default).
# Falls back to Whisper automatically if not installed.
# onnxruntime
# torch
# indic-asr-onnx
